from django.apps import AppConfig


class PostsConfig(AppConfig):
    name = 'posts'
    # Тут можно указать, например, поле Любое_name 
    # под этим именем приложение будет видно в админке.
    # пример verbose_name = 'Управление сортами мороженого' 
